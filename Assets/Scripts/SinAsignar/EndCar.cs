using UnityEngine;

public class EndCar : MonoBehaviour
{
    
    /*
     
   [SerializeField] AudioSource _audioSource;
   [SerializeField] AudioClip _clip;

  
    private void Update()
    
   {
       _audioSource.Play();
       Destroy(this,5f);
       
       agregar que al presionar el auto se ponga la pantalla negra y se reproduzca el sonido
   }
   
  */
    
}
